import { createNamespacedHelpers } from 'vuex'
 const {  mapState, mapMutations} = createNamespacedHelpers('global')
 export const globalMapState = mapState
 export const globalMapMutations = mapMutations